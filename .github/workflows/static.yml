 1 # Simple workflow for deploying static content to GitHub
      Pages
    2 name: Deploy static content to Pages
    3
    4 on:
    5   push:
    6     branches: ["main"]
    7   workflow_dispatch:
    8
    9 permissions:
   10   contents: read
   11   pages: write
   12   id-token: write
   13
   14 jobs:
   15   deploy:
   16     environment:
   17       name: github-pages
   18       url: ${{ steps.deployment.outputs.page_url }}
   19     runs-on: ubuntu-latest
   20     steps:
   21       - name: Checkout
   22         uses: actions/checkout@v4  # This line needs 8 spaces
      of indentation
   23       - name: Setup Pages
   24         uses: actions/configure-pages@v4 # This line needs 8
      spaces of indentation
   25       - name: Upload artifact
   26         uses: actions/upload-pages-artifact@v3
   27         with:
   28           # Upload the 'client' directory
   29           path: './client'
   30       - name: Deploy to GitHub Pages
   31         id: deployment
   32         uses: actions/deploy-pages@v4
